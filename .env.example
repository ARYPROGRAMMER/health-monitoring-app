# Copy to .env. Read at runtime by AppConfig.
# Leave API_BASE_URL blank to use the built-in default; or point it at your
# backend (the /api suffix is added automatically).
API_BASE_URL=https://your-backend.example.com
POLL_INTERVAL_SECONDS=60
DEFAULT_TIMEOUT_SECONDS=45
ENABLE_VERBOSE_LOGGING=false
